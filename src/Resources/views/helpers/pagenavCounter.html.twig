<div class="pagenav-counter">
    {% set num = entities.paginationData.current %}
    {% set all = entities.paginationData.last %}

    {{ 'Page %num% of %all%'|trans|replace({ '%num%': num, '%all%': all }) }}
</div>
